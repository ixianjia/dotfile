-- ============================================================
-- 主题配色配置
-- ============================================================
-- 使用 Tokyo Night 主题
-- priority = 1000 确保主题最先加载
-- ============================================================

return {
  "folke/tokyonight.nvim",
  priority = 1000,  -- 最高优先级，确保主题最先加载
  config = function()
    vim.cmd.colorscheme("tokyonight")  -- 激活 Tokyo Night 主题
  end,
}
