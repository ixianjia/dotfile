-- ============================================================
-- Comment 代码注释插件配置
-- ============================================================
-- 提供快速注释/取消注释代码的功能
-- 支持单行注释和块注释，自动识别语言注释风格
-- ============================================================

return {
  "numToStr/Comment.nvim",
  event = { "BufReadPre", "BufNewFile" },  -- 打开或创建文件时加载
  config = function()
    -- 使用默认配置即可，插件会自动识别不同语言的注释风格
    require("Comment").setup()
  end,
}
